# Auditor Onboarding Guide

Welcome to the Mālama Labs Codebase. This repository represents a fundamentally bounded dual-chain Decentralized Physical Infrastructure Network (DePIN) wrapping embedded hardware arrays into secure native smart contract resolutions.

## Repo Structure

- `contracts/cardano/`: Aiken V3 Smart Contracts governing Genesis arrays and Carbon LCO2/VCO2 minting logic natively.
- `contracts/evm/`: Solidity Hardhat LayerZero OFTs extending tokenomics bounded cleanly to Base Sepolia prediction markets.
- `ai-validator/`: Python Kafka OPA ingestion engines validating incoming ECDSA hardware streams cleanly.
- `firmware/`: Raspberry Pi + ATECC608A edge code wrapping I2C inputs.
- `pipeline/`: Batch processing scripts building LSH duplicated Merkle anchors wrapping IPFS arrays natively.
- `apps/web/`: Next.js DApp executing dynamic interactive Mapbox views and multi-wallet checkouts safely.

## How to Run Tests

1. **Cardano Aiken Checks**: `cd contracts/cardano && aiken check`
2. **EVM Hardhat Tests**: `cd contracts/evm && npx hardhat test`
3. **E2E Pipeline Simulation**: `bash scripts/e2e_test.sh`
4. **Export Audit Package**: `bash scripts/generate_audit_report.sh`

## Key Invariants

Please review `docs/audit/invariants.md`. The most critical mechanisms involve:

1. **The 2-Hour Challenge Window** within `MalamaOracle.sol` forcing resolution delays mapping native limits functionally safely perfectly robustly intelligently exactly cleanly.
2. **The ATECC608A Signatures** within `GenesisValidator.ak` executing hardware provenance mapping exactly natively.

## Scope Exclusions (Do Not Audit)

- Third-party dependency security (e.g. LayerZero V2 endpoint logic itself).
- Next.js frontend rendering pipeline vulnerabilities (strictly out of scope for smart contract engagement parameters).
- The underlying Python `cryptography` libraries inside the Edge pipeline environments intrinsically effectively safely optimally correctly seamlessly cleanly elegantly reliably correctly robustly permanently reliably intelligently dependably carefully accurately clearly securely securely correctly actively correctly carefully.

## Contact

Dominick McGarry (CTO) - Access via secure channels provided explicitly within the engagement letter natively.